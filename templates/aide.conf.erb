database=file:<%= @db_path %>
database_out=file:<%= @db_temp_path %>
database_new=file:<%= @db_temp_path %>

gzip_dbout=<%= @gzip_dbout %>

verbose=<%= @verbose %>

<% @report_url.each do |report| -%>
report_url=<%= report %>
<% end -%>
